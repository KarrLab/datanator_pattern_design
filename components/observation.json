{
    "bsonType": "object",
    "type": "object",
    "required": ["entity", "source", "schema_version"],
    "properties": {
        "entity": {"$ref": "entity.json"},
        "values": {
            "bsonType": "array",
            "type": "array",
            "items": {"$ref": "value.json"}
        },
        "genotype": {"$ref": "genotype.json"},
        "environment": {"$ref": "environment.json"},
        "source": {"$ref": "identifier.json"},
        "schema_version": {
            "bsonType": "string",
            "type": "string"
        }
    }
}